# Point the frontend at the backend API so DEFAULT_OAUTH_REDIRECT_URI
# is computed as http://localhost:8000/api/auth/cloudreve/callback
# — matching what is registered in Cloudreve's OAuth application.
VITE_NEXUS_API_BASE=http://localhost:8000
